import React from 'react';

import './header.css';

const Header = () => {
    return(
        <header id='home-header'>
            <a href="#home">Home</a>

        </header>

    );
}

export default Header;
